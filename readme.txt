DBCleaner Moodle Tool
==============================

This tool records description of foreing keys and applies an orphan check and delete
over those description.

A standard set of description are built-in into the tool for core cleaning. 

Some extra foreign key deifnitions might be loaded manually or from components
to handle non standard components
